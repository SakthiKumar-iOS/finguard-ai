# utils/prompt_guard.py
# Purpose: Guard against prompt injection and malicious inputs.

def check_injection(text: str) -> bool:
    """Check if the text contains potential prompt injection."""
    return False
